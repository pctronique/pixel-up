class EnumCollision {
  static NULL = -1;
  static HAUT = 0; // Collision en haut.
  static BAS = 1; // Collision en bas.
  static GAUCHE = 2; // Collision à gauche.
  static DROITE = 3; // Collision à droite.
}
