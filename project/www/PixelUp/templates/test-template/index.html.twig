{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}


<!--Fichier à supprimier-->
    <h1>Utilisateur index</h1>

    <table class="table">
        <thead>
            <tr>
            
                <th>Identifiant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
             
                <td>{{ user.username }}</td>
                <td>
                    <a href="{{ path('app_user_show', {'id': user.id}) }}">Afficher le profil</a>
                    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Édition</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}


 <h1>Utilisateur</h1>
{% for user in users %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Identifiant</th>
                <th>Role</th>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
               
            </tr>
        </tbody>
    </table>

        {{ include('user/_delete_form.html.twig') }}