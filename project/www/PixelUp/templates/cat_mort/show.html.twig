{% extends 'base.html.twig' %}

{% block title %}CatMort{% endblock %}

{% block body %}
    <h1>CatMort</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_mort.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cat_mort.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_mort_index') }}">back to list</a>

    <a href="{{ path('app_cat_mort_edit', {'id': cat_mort.id}) }}">edit</a>

    {{ include('cat_mort/_delete_form.html.twig') }}
{% endblock %}
