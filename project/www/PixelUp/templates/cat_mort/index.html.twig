{% extends 'base.html.twig' %}

{% block title %}CatMort index{% endblock %}

{% block body %}
    <h1>CatMort index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cat_mort in cat_morts %}
            <tr>
                <td>{{ cat_mort.id }}</td>
                <td>{{ cat_mort.nom }}</td>
                <td>
                    <a href="{{ path('app_cat_mort_show', {'id': cat_mort.id}) }}">show</a>
                    <a href="{{ path('app_cat_mort_edit', {'id': cat_mort.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_cat_mort_new') }}">Create new</a>
{% endblock %}
