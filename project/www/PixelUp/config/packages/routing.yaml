framework:
    router:
        utf8: true

        # Configurez comment générer des URL dans des contextes non HTTP, tels que les commandes CLI.
        # See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
        #default_uri: http://localhost

when@prod:
    framework:
        router:
            strict_requirements: null
