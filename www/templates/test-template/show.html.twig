{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}

<!--Fichier à supprimier-->
    <h1>Utilisateur</h1>
{% for user in users %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Identifiant</th>
                <th>Role</th>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
               
            </tr>
        </tbody>
    </table>

{% endfor %}  

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
